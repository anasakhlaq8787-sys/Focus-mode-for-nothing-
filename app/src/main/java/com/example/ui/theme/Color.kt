package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Sophisticated Dark Aesthetic Colors - Redesigned for Nothing OS Mono & Red Dot Accent
val SophisticatedBg = Color(0xFF000000)         // Pure OLED Pitch Black background
val SophisticatedSurface = Color(0xFF121212)    // Dark Matte Charcoal Widget Cards
val SophisticatedOutline = Color(0xFF262626)    // Subtle Dark Grey Accent borders (Nothing thin dividers)
val SophisticatedPrimary = Color(0xFFFFFFFF)    // Monochromatic Silver-White focus text / action brand
val SophisticatedMuted = Color(0xFF888888)      // Muted Nothing OS system info labels (grey)
val SophisticatedWhite = Color(0xFFF5F5F5)      // Bright text for readouts
val SophisticatedRoseRed = Color(0xFFFF003C)    // Iconic Nothing Red Active Pixel / LED Dot / Cancel accent
val SophisticatedDarkRose = Color(0xFF1F0007)   // Deep red-black cherry contrast overlay
